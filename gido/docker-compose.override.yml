# 可选：GIDO backend 加入 Kind 网络（仅 Docker backend + Kind 集群 Operator 提交时需要）
# 用法：cp docker-compose.kind.override.yml.example docker-compose.override.yml
#
services:
  backend:
    networks:
      - default
      - kind

networks:
  kind:
    external: true
    name: kind
