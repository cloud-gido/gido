# Flink 1.17.2 运行时（Operator Profile / 平台默认示例）
#
# 适用：已有 Flink 1.17.2 自定义镜像（含业务 connector / sql-runner），Operator CRD 须支持 v1_17。
# 与默认 2.2.1 路径并存：通过 Operator 集群 Profile 或作业级 runtime_image 选择 1.17 镜像。
#
# 用法：合并进仓库根 .env，或在 K8s ConfigMap 中设置同名 GIDO_* 变量。

# 平台默认（可选；也可只在 Operator Profile 中配置）
# GIDO_FLINK_OPERATOR_IMAGE=your-registry/flink:1.17.2-java11
# GIDO_FLINK_OPERATOR_FLINK_VERSION=v1_17
# GIDO_FLINK_K8S_APPLICATION_IMAGE=your-registry/flink:1.17.2-java11

# Operator Profile API 示例（POST /api/streaming/flink-operator-profiles）：
# {
#   "workspace_id": 1,
#   "name": "flink-1.17",
#   "flink_operator_namespace": "flink",
#   "flink_operator_image": "your-registry/flink:1.17.2-java11",
#   "flink_operator_flink_version": "v1_17"
# }
#
# 仅填镜像、不填 flink_operator_flink_version 时，GIDO 会从 tag 1.17.2 自动推断 v1_17。
