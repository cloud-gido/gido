# K3s 节点配置片段：禁用 containerd 对 registry 的 HTTPS 默认回退
# 合并到节点 /etc/rancher/k3s/config.yaml 后执行: sudo systemctl restart k3s
#
# 与 k3s-insecure-registry.yaml（mirrors）配合，解决 HTTP registry 仍走 https:// 的 EOF。
disable-default-registry-endpoint: true
